# x-bookmark-miner environment variables
# Copy to .env or .env.local (both are git-ignored)

# Gemini API key for AI categorization (optional — keyword fallback works without it)
# Get a free key at: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here
